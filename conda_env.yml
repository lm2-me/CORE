name: CORE
channels:
- conda-forge
- defaults
dependencies:
- python=3.10.6
- numpy=1.23.3
- matplotlib=3.5.3
- networkx=2.8.6
- flask=2.2.2
- pip=22.2.2
- osmnx=1.2.2
- pip:
  - ghhops-server==1.5.3
  - taxicab==0.0.3

#if you run " conda env update --prefix ./env --file conda_env.yml  --prune " 
#then it will install pip and ghhops-server without needing to install seperately

# Please install through pip:
# - ghhops-server=1.5.3
# If you use an environment, please first install pip
# Check conda_cheat.txt to install or upgrade pip